version: '3.0'
services:
  python:
    ports:
    - ${PYTHON_PORT}:${PYTHON_PORT}
    volumes:
    - ${VOLUME_HOME}/python3.6.5:/root/python3.6.5
    - ${VOLUME_HOME}/.bashrc:/root/.bashrc
    command:
    - /bin/sh
    - -c
    - "${INIT_COMMAND}"
    restart: always
    image: python:3.6.5
