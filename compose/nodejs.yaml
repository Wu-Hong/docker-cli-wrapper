version: '3.0'
services:
  nodejs:
    ports:
    - "${NODEJS_PORT}:${NODEJS_PORT}"
    volumes:
    - ${VOLUME_HOME}/nodejs:/root/nodejs
    - ${VOLUME_HOME}/.bashrc:/root/.bashrc
    image: node:latest
    command:
    - /bin/sh
    - -c
    - "${INIT_COMMAND}"
    restart: always
